import styled, { css } from "styled-components";

const StyledProfilePic = styled.img`
    width: 8rem;
    max-height: 8rem;
    border-radius: 10%;

    :hover {
    }
`;

export { StyledProfilePic };
